package elasticsearch

// defaultClusterName is the cluster name used when none is configured, mirroring
// kafka.defaultClusterName / storage.defaultBucketName.
const defaultClusterName = "elasticsearch"
